<%= form_with(model: discussion) do |form| %>
  <% if discussion.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(discussion.errors.count, "error") %> prohibited this discussion from being saved:</h2>

      <ul>
        <% discussion.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <% current_user = User.find(1) %>
    <%= form.number_field :user_id, id: :story_user_id, value: current_user.id, type: :hidden %>
  </div>

  <div>
    <%= form.text_field :movie_id, id: :movie_id, value: @movie.id, type: :hidden %>
  </div>

  <div class="input-group mb-3">
    <%= form.text_field :title, class: "form-control" %>
    <%= form.submit class: "btn btn-primary" %>
  </div>

<% end %>
